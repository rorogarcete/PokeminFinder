<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:theme="@style/AppTheme.ActionBar"
            android:contentInsetStart="0dp">

        </android.support.v7.widget.Toolbar>

    <FrameLayout
            android:id="@+id/container_types"
            android:layout_width="match_parent"
            android:layout_height="@dimen/frame_type_dim"
            android:layout_below="@+id/toolbar"/>

    <FrameLayout
            android:id="@+id/container_pokemons"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@+id/container_types"
            android:layout_marginBottom="@dimen/margin_8dp"/>

    <include android:id="@+id/progress_indicator" layout="@layout/progress_view"/>

</RelativeLayout>